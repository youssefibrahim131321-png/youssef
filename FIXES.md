# إصلاحات العيوب الـ 12

| # | العيب | الإصلاح |
|---|-------|---------|
| 1 | تأكيد الدفع اليدوي قابل للتزوير/التكرار | رقم عملية التحويل بقى **فريد**: أي طلب بنفس الرقم بيترفض (`getOrderByTransferRef` + تحقق في إنشاء الطلب)، مع تحذير واضح للأدمن قبل التأكيد |
| 2 | إمكانية قفل حساب أي مستخدم (DoS) | حدّ المحاولات بقى **على الفشل** مش على كل محاولة، مربوط بالحساب + الـ IP، وبيتصفّر فورًا عند نجاح الدخول |
| 3 | الجلسة ما بتتلغيش بعد الخروج | كل جلسة ليها `jti` فريد + جدول `revoked_sessions`؛ الخروج بيبطّل الكوكي المسروقة فورًا، وفيه تنظيف دوري للمنتهي |
| 4 | نسخة واحدة فقط (مافيش scaling) | وضع **leader/follower**: أي عدد نسخ على نفس الـ Volume (SQLite WAL + busy_timeout)، والنسخة صاحبة القفل بس هي اللي بتشغّل المهام الدورية؛ حدود المعدّل بتتخزّن في القاعدة المشتركة |
| 5 | النسخ الاحتياطي محلي وممكن يضيع مع السيرفر | الإنتاج بيرفض يقلع من غير `BACKUP_UPLOAD_URL` إلا لو `ALLOW_LOCAL_ONLY_BACKUP=1` صراحةً |
| 6 | فشل صامت للإيميلات (تفعيل/استعادة كلمة السر) | مزوّد بريد مطلوب في الإنتاج، وإلا السيرفر ما بيقلعش |
| 7 | تشفير كلمة السر كان بيوقف الـ event loop | نسخ **غير معطِّلة** (`hashPasswordAsync` / `verifyPasswordAsync` / `createUserAsync`) في كل مسارات الدخول والتسجيل وتغيير كلمة السر |
| 8 | CSP فيها `unsafe-inline` للأنماط | الأنماط الداخلية بقت بـ **nonce** زي السكريبتات، و`style-src` بقى بدون `unsafe-inline` |
| 9 | تسميم هيدر Host في الروابط | `siteBaseUrl()` بيعتمد `SITE_URL` أولًا وبيتحقق من `ALLOWED_HOSTS` |
| 10 | مسار مكسور بيرجّع 500 | فك ترميز آمن + معالج أخطاء بيرجّع **400** للطلبات الغلط بدل 500 |
| 11 | `server.js` ملف عملاق | اتفصل: `lib/session-layer.js` (الجلسات و CSRF) و`lib/security-headers.js` (رؤوس الأمان و CSP) |
| 12 | مافيش CI ولا متابعة ثغرات | `.github/workflows/ci.yml` (lint + فحص XSS + اختبارات + `npm audit`) و Dependabot أسبوعي، و`npm run check` محليًا |

## متغيّرات جديدة
شوف `.env.example` — أهمها `SITE_URL`, `ALLOWED_HOSTS`, `BACKUP_UPLOAD_URL`, `ALLOW_MULTI_INSTANCE`.

## التحقق
```
npm ci && npm run check      # lint + XSS + 50 اختبار + فحص ثغرات
```
